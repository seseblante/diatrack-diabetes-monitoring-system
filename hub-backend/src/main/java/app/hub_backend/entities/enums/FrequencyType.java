package app.hub_backend.entities.enums;

public enum FrequencyType {
    DAILY,      // e.g., 1 time, 2 times, etc.
    WEEKLY,     // e.g., every 1 week
    MONTHLY,    // e.g., every 1 month
    CUSTOM_DAYS // e.g., every 14 days
}